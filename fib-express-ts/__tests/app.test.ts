describe('App Test', () => {
    test('GET /random-url should return 404', () => {
        expect(true).toBeTruthy();
    });

    test('GET /book/all should return 200', () => {
        expect(2 + 3).toBe(5);
    });
});
